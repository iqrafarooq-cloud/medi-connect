node-linker=isolated
# This machine's link to the registry is slow and drops parallel connections;
# fewer sockets with longer timeouts completes far more reliably than the default 16.
network-concurrency=3
fetch-retries=6
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=120000
fetch-timeout=300000
